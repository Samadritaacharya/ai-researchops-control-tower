"""pytest configuration.

The presence of this file at the project root makes pytest add the root to
``sys.path`` (prepend import mode), so ``from utils.scoring import ...`` works
in the test modules without any extra setup.
"""
