name: Archived Temporary Publish Workflow

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: No operation
        run: echo "Project files are now published directly. Use the CI workflow for validation."
